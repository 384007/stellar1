# Example keys for Modal Secret named ``custom-secret`` (see modal_app.py).
# Do not commit real URLs to git.
#
# 说明：代理地址不会写在代码仓库里；放在 Cloudflare Pages 与 Modal ``custom-secret``（同名键，值一致）。
# Pro v3 文案报告：先 Gemini（直连 / ALI / JD），失败再用 QWEN_API_KEY 通义兜底。
#
# GEMINI_PROXY_* = HTTPS 根地址，反代 generativelanguage.googleapis.com 的 REST 路径，无末尾 /。

GEMINI_PROXY_ALI=
GEMINI_PROXY_JD=
